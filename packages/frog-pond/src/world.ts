// The simulation on its own (no DOM): step it, tap it, check it. See frog-world.ts.
export {
  FrogWorld,
  type Firefly,
  type Frog,
  type Mote,
  type Pad,
  type RippleEmitter,
  type TapResult,
} from './frog-world';
