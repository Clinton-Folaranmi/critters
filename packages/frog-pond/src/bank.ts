// The shore maths on its own (no DOM): where the water is, how deep, where
// the reeds stand, and the picture's outline. See frog-bank.ts.
export {
  WATER_CX,
  WATER_CY,
  openDistance,
  pictureEdge,
  reedAt,
  reedBlades,
  shelfDepth,
  shoreDistance,
} from './frog-bank';
